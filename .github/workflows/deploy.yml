name: Sync to Hugging Face Spaces

on:
  push:
    branches: [main] # Ganti jadi 'master' jika branch utamamu namanya master

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
      with:
        fetch-depth: 0
    - name: Push to HF
      env:
        HF_TOKEN: ${{ secrets.HF_TOKEN }}
      run: |
        git config --global user.name "GitHub Actions"
        git config --global user.email "actions@github.com"

        git rm -rf frontend
        git commit -m "chore: exclude frontend assets for HF space"

        git remote add hf https://root:$HF_TOKEN@huggingface.co/spaces/Ooyaann/webcraft-db-hf
        git push --force hf main